using UnityEngine;

public class DockingPoint : MonoBehaviour
{
    public Transform shipDockPosition; // Posição fixa para o navio
    public Transform playerSpawnPosition; // Posição fixa para o jogador na terra
}
